# Yield Curve Visualizer

Streamlit app for exploring U.S. Treasury daily yield curves.

## Run locally

```bash
cd "yield-curves-app"
pip install -r requirements.txt
streamlit run app/yield_curves_app.py
```
